<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Payment Received - Order Completed</title>
  </head>
  <body style="margin: 0; padding: 20px; background-color: #f4f4f4">
    <div
      style="
        max-width: 900px;
        margin: auto;
        background: white;
        padding: 20px;
        border-radius: 8px;
        border: 1px solid #d4e0df;
      ">

      <!-- Logo -->
      <table style="width: 100%; text-align: center">
        <tr>
          <td>
            <img src="<%= adminData.logo %>" style="width: 100px" />
          </td>
        </tr>
      </table>

      <!-- Content -->
      <table style="width: 100%; margin-top: 30px">
        <tr>
          <td>
            <h2 style="color: #28a745; font-weight: 600">
              Payment Received 🎉
            </h2>

            <p>
              Good news! The customer has successfully paid for this order.
            </p>

            <p>
              This delivery has now been marked as <strong>completed</strong>.
            </p>

            <p><strong>Tracking ID:</strong> <%= order.tracking_id %></p>
            <p><strong>Booking ID:</strong> <%= order.booking_id %></p>
            <p><strong>Pickup Time:</strong> <%= order.picked_time %></p>
            <p><strong>Your Name:</strong> <%= rider.full_name %></p>
          </td>
        </tr>
      </table>

      <!-- Locations -->
      <table style="width: 100%; margin-top: 10px">
        <tr>
          <td style="width: 50%; padding: 10px">
            <strong>Pickup Location</strong><br />
            <%= order.source_address %>, <%= order.source_city %><br />
            <%= order.source_postcode %>
          </td>
          <td style="width: 50%; padding: 10px">
            <strong>Drop-off Location</strong><br />
            <%= order.dest_address %>, <%= order.dest_city %><br />
            <%= order.dest_postcode %>
          </td>
        </tr>
      </table>

      <!-- Parcels -->
      <table style="width: 100%; margin-top: 20px; border: 1px solid #ddd">
        <thead>
          <tr style="background: #28a745; color: white">
            <th style="padding: 12px">Parcel</th>
            <th style="padding: 12px">Source</th>
            <th style="padding: 12px">Destination</th>
          </tr>
        </thead>
        <tbody>
          <% order.parcels.forEach(parcel => { %>
          <tr>
            <td style="padding: 12px">
              <%= parcel.parcel_number %>
            </td>
            <td style="padding: 12px">
              <%= parcel.source_address %>
            </td>
            <td style="padding: 12px">
              <%= parcel.destination_address %>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>

      <p style="margin-top: 20px">
        Thank you for completing the delivery successfully.
      </p>
    </div>
  </body>
</html>